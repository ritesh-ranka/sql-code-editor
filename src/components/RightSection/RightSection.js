import React, { memo } from "react";

const RightSection = () => {
  return (
    <div style={{ height: "100%", display: "flex", border: "1px solid black" }}>
      RightSection
    </div>
  );
};

export default memo(RightSection);
